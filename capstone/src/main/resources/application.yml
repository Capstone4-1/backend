spring:
  autoconfigure:
    exclude:
      - io.awspring.cloud.autoconfigure.s3.S3AutoConfiguration

  jwt:
    secret: qwertyuiopasdfghjklzxcvbnmqwertyuifgHJDDGHDGDjshdjsd
    access-token-expiration: 1800000 # 30분 (ms)
    refresh-token-expiration: 604800000 # 7일 (ms)

  datasource:
    url: jdbc:h2:tcp://localhost/~/capstone
    #url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver


  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none #Application 실행 시점에 Entity 삭제 & 재생성
    properties:
      hibernate:
        show_sql: true #생성한 SQL 이 다 보임 :) System out 을 통해 출력.
        format_sql: true

cloud:
  aws:
    region:
      static: ap-northeast-2
    credentials:
      access-key: AKIA6FZA3SFAYUSZSANJ
      secret-key: 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
    s3:
      bucket: 2025-capstone-moai-bucket

logging:
  level:
    org.hibernate.SQL: debug #sql 을 전부 DEBUG 모드로 남긴다는 뜻. Logger 를 통해.
    org.hibernate.orm.jdbc.bind: trace


server:
  address: 0.0.0.0
  port: 8080



